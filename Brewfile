# Brewfile for FetchCord macOS dependencies
# Usage: brew bundle

# Required for system information detection
brew "fastfetch"

# Python 3.10+ (optional - if you need a newer Python version)
# brew "python@3.12"
